import { StatelessComponent } from 'react';
import ThemeType from '../../types/ThemeType';
declare type PropsType = {
    theme: ThemeType;
};
declare const MerchantTheme: StatelessComponent<PropsType>;
export default MerchantTheme;
export { PropsType };
